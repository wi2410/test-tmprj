import React from 'react';
import Lottie from 'lottie-react';
import * as loading from '../../img/loading-cat.json';

const Loading = () => <Lottie animationData={loading} loop={true} />;

export default Loading;
